# Frontend (React + Vite)

The website for Call Transcript Search.

## Run

```bash
npm install
npm run dev              # http://localhost:5173
```

The backend must be running on http://localhost:3000 (see `../backend`).
The API URL is set in `.env` (`VITE_API_URL`).
